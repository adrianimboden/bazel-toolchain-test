# For future reference

This example builds the same c++ code with multiple different toolchains/platforms.

It uses transitions via `with_cfg.bzl`.

`hermetic_cc_toolchain` is used for this example, but can be replaced with the platforms/toolchains of your choice.
